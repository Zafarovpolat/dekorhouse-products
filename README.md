# DekorHouse Products

Primary repository for approved edited product images and the printable ZELEN catalog.

**Before changing Bito, Supabase, images, or PDF files, read [`PROJECT_CONTEXT.md`](PROJECT_CONTEXT.md).**

Upload tooling is documented in [`scripts/README.md`](scripts/README.md).

Real credentials are never stored in this repository. Copy `.env.example` to the ignored `.env.local` and request fresh keys from the owner.
